# `etcd-entry-size`

---

`etcd-entry-size` attempts to estimate how much space an object is occupying in the `etcd` database.

This can help you tune usage of certain resources in the Kubernetes API.
